import React from 'react';
import { StyledDiv } from "./styles";

const StatsBar = () => {
    return (
        <StyledDiv>
            StatsBar
        </StyledDiv>
    );
};

export default StatsBar;