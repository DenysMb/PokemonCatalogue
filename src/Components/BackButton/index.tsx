import React from 'react';
import { StyledDiv } from "./styles";

const BackButton = () => {
    return (
        <StyledDiv>
            BackButton
        </StyledDiv>
    );
};

export default BackButton;